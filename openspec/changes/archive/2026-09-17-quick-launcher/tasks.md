## 1. 规格与数据

- [x] 1.1 完成快速启动器变更规格并通过 OpenSpec 严格校验
- [x] 1.2 定义快捷方式数据结构、默认入口和 HTTP/HTTPS 校验规则，并通过类型检查验证

## 2. 启动器交互

- [x] 2.1 实现快捷方式列表、搜索过滤和无结果状态，并通过浏览器检查匹配结果
- [x] 2.2 实现点击和 Enter 新标签页打开网址，并通过浏览器交互检查
- [x] 2.3 实现新增、删除和表单校验，并通过浏览器检查有效提交与无效提交
- [x] 2.4 实现 `localStorage` 保存/恢复和 `/` 键聚焦搜索框，并通过刷新和键盘交互检查

## 3. 视觉与交付

- [x] 3.1 保留顶部导航栏并完成响应式基础样式，确认导航栏之外只有启动器内容
- [x] 3.2 执行生产构建并确认命令成功退出
- [x] 3.3 启动生产应用并完成首页 HTTP 与浏览器检查
- [x] 3.4 更新 OpenSpec 任务、归档变更、提交并推送到 `origin/main`
