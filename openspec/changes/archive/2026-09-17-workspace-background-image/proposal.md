# Proposal: 工作区背景图片

## Why

用户提供了一张本地图片，希望将其纳入 StudyPower 程序文件夹，并作为左侧导航栏右侧空白工作区的背景。

## What Changes

- 将 `wallhaven-28d516.jpg` 纳入仓库的 `public/` 静态资源目录。
- 为右侧 `.workspace` 设置居中、等比覆盖的背景图片。
- 保持左侧导航和右侧工作区的空内容边界不变。

## Non-Goals

- 不修改图片内容，不生成裁剪副本或第二份资源。
- 不修改左侧导航、路由、统一启动器或其他项目。
